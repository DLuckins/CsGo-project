package com.company;


import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class Main {

    public static void main(String[] args) {

        float factoryNew = 0.035f;
        float minimalWear = 0.11f;
        float fieldTested = 0.26f;
        float wellWorn = 0.41f;
        float battleScarred = 0.725f;
        List<String> sufficientCollections = new ArrayList<String>();
        List<String> skinValues = new ArrayList<String>(Arrays.asList("Collection", "Wear", "Tier" ,"Price", "ROI", "s", "next tiers wear", "Calculated gap"));
	    //searches and finds the biggest gap
        float gap = 0f;
        if (gap>0){}
        //Get the item values- Skin name, skin condition, condition to aim for, collection the skin is in
        //skins price, skins price in next condition and calculate or get the gap, how many different outcomes there are
        //calculate the ROI that is needed for tradeup to be profitable
        //search the database and check if there even are collections for the needed tier with ROI that is eaqual or higher than needer ROI

        //stores the collections in a list
        //sufficientCollections.add();

        //gets the collection and finds the items that
        int sufficientCollectionSize = sufficientCollections.size();
        //List<String>
        for (int i = 0; i < sufficientCollectionSize; i++) {
            String collection = sufficientCollections.get(i);

        }

        //Check if the
    }
}
